# للوكلاء (اقرأ هذا أولاً — مختصر)

عند رسالة المستخدم: **«تم تحديث ملف التصميم»** → نفّذ `AGENT_COMMAND.md` دون طلب تفاصيل إضافية.

| اقرأ | لا تقرأ إلا عند الحاجة |
|------|-------------------------|
| `VERSION` ثم `AGENT_COMMAND.md` | دليل HTML الطويل / screenshots |
| `package.json` (رقم الوسم) | كل `components.md` دفعة واحدة |
| قسم العقد للمكوّن الذي تلمسه فقط من `package/components.md` | نسخ محلية قديمة للتصميم |

المصدر: `@zaad/design-system` = مجلد `package/` في هذا المستودع.
