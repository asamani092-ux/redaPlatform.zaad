# منصة رداء (`web`)

```bash
cp .env.example .env
npm install
npx prisma db push
npm run db:seed
npm run dev
```

المنفذ الافتراضي: **http://localhost:3100** (لتفادي التعارض مع مشاريع على 3000).

دخول المدير: `0500000000` / `Admin@1234`
